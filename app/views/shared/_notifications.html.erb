<script>
  document.addEventListener("DOMContentLoaded", () => {
    <% flash.each do |type, data| %>
      notify("<%= type %>", "<%= j data[:message] %>", "<%= j data[:title] %>", "<%= j data[:duration] %>");
    <% end %>
  });
</script>
